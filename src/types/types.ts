export interface StyleProps {
    style: { readonly [key: string]: string };
    isOpen?: boolean;
    onClick?: () => void;
}

export interface InputProps {
    style: { readonly [key: string]: string };
    isOpen?: boolean;
    onClick?: () => void;
}
